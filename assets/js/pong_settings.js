// this file only contains variables that we use as settings
// such as font styles and other variables
// purpose: make it easy to change settings in the game in one place

var textStyleNormal = { font: '25px Arial', fill: '#ffffff' };
var textStyleHeader = { font: '50px Arial', fill: '#ffffff' };
var textStyleWin = { font: '50px Arial', fill: '#00FF00' };
var playerStartX = 20;
var playerStartY = game.height/2;

var winStartX = game.width-20;
var winStartY = game.height/2;